# 《Openspace OS Core 开发任务书》 v1.1

**项目名称**：Openspace OS Core（开源核心）  
**文档版本**：v1.1（细化版）  
**编制日期**：2026年7月  
**所属项目**：Openspace OS（开源太空联盟子联盟/工作组）

---

## 1. 项目概述

### 1.1 项目定位
Openspace OS Core 是整个 Openspace OS 平台的**基础运行时和公共基础设施**。它采用完全开源模式，目标是成为商业航天领域可被广泛复用的软件底座，为上层商业模块和第三方应用提供统一的对象模型、事件驱动能力、插件框架以及基础的遥测/遥控能力。

### 1.2 项目目标
- 构建一个**可独立部署和运行**的太空操作核心平台；
- 建立清晰的领域模型（含知识图谱）和事件驱动架构；
- 支持从单星/小星座到大规模星座的平滑扩展；
- 为生态贡献者提供稳定、易用的插件开发框架和多语言 SDK；
- 作为开源太空联盟的重要工作组产出，推动行业标准化。

### 1.3 核心约束
- Open Core **必须能够独立运行**（standalone），至少支持基础的卫星监控与控制功能。
- 必须遵循《Openspace OS 顶层设计白皮书》中的所有核心原则，特别是 **Planetary-Scale Compatibility**、**Event-Driven First** 和 **Knowledge Graph 优先**。
- 接口契约先行：核心对象模型和事件 schema 必须先定义，再进行功能开发。

---

## 2. 核心原则与要求

### 2.1 必须遵循的原则
- **Planetary-Scale Compatibility**：架构设计需支持百万级规模（分层联邦、事件分片、自治梯度）。
- **Event-Driven First**：所有状态变更必须通过事件发布与订阅实现。
- **Knowledge Graph 优先**：Node 模型和知识图谱作为 Single Source of Truth 尽早落地。
- **Plugin-First**：核心保持轻量，扩展能力主要通过插件实现。
- **接口契约先行**：先定义稳定的接口和事件 schema，再开发实现。

### 2.2 Phase 1 架构扩展性设计要求（新增）

即使在 MVP 阶段，也必须为未来大规模扩展做好设计准备：

- **领域模型**：Node 类型和关系设计必须预留分片键（Shard Key）和联邦标识，支持后续水平拆分。
- **事件驱动框架**：Event Bus 必须采用可替换的消息中间件抽象层，便于后期升级为分层 Event Mesh。
- **存储层**：必须设计存储抽象接口，支持从单机数据库平滑迁移到分布式存储。
- **Telemetry / Command Pipeline**：必须支持按卫星、按区域、按 Community 进行逻辑分片，为后续物理分片做准备。
- **无状态优先**：核心服务应尽量保持无状态，状态通过事件和知识图谱管理。

---

## 3. 领域模型要求（Knowledge Graph 相关）

领域模型是整个系统的根基，必须在早期阶段重点投入。

**必须完成的内容**：
- 定义核心 **Node** 类型（Satellite、GroundStation、Antenna、Payload、Mission、Task、Telemetry、Command、Alarm、Community 等）及其属性、状态机和关系。
- 设计 **Knowledge Graph** 的基础能力（支持节点注册、关系维护、基础查询）。
- 定义核心事件 schema（TelemetryReceived、StateUpdated、HealthAlarm、TaskScheduled、NodeRegistered 等）。
- 确保对象模型与事件模型紧密关联，所有状态变更都有对应事件。

**扩展性要求**：领域模型设计必须支持后续分片和联邦查询，不得在后期进行大规模重构。Node 模型需包含可用于分片的标识字段。

---

## 4. 分阶段开发规划

### Phase 1：MVP（基础可用版）
**目标**：构建可独立运行的 Open Core 最小闭环，支持单星或小星座的基础监控与控制。

**核心交付内容**：
- 统一 Space Object Model（核心 Node 类型）
- 基础 Knowledge Graph 实现（单节点，支持基础关系查询）
- 事件驱动框架（本地 Event Bus，支持可扩展抽象）
- Plugin Framework（基础版本，支持协议插件加载与运行）
- Telemetry Pipeline（支持逻辑分片设计）
- Command Pipeline（基础队列与确认机制）
- 基础 Identity 与权限模型
- Python + Go SDK（最小可用版本）
- CLI 工具
- 支持 Windows 和 Linux 单机部署

**成功标准**：
- 能够注册卫星和地面站节点；
- 能够接收遥测并更新卫星状态；
- 能够下发简单遥控指令并收到确认；
- 支持至少一种协议插件加载运行。

### Phase 2：社区可用版
**目标**：完善插件框架、支持基础联邦能力，形成可被社区贡献的生态。

**重点方向**：
- Event Mesh 基础联邦能力
- Knowledge Graph 能力增强（复杂关系查询）
- Plugin Framework 成熟版（沙箱、版本管理、依赖解析）
- 多语言 SDK 完善
- 兼容性测试套件与认证机制
- 基础可视化与管理界面
- 完善开发者文档与贡献指南

### Phase 3：大规模支撑版
**目标**：支撑万星级规模，为 Commercial Modules 提供坚实基础。

**重点方向**：
- 分层 Event Mesh 与分片支持
- Knowledge Graph 联邦查询能力
- 分布式部署与高可用
- 性能优化（百万级事件处理能力）
- 高级可观测性（分布式追踪）

---

## 5. Phase 1 详细任务拆解（MVP）

以下任务按建议优先级排序：

### 5.1 领域模型与 Knowledge Graph 基础（最高优先级）

**任务目标**：建立统一的 Node 模型和基础知识图谱能力，并为未来扩展做好设计。

**具体子任务**：
- 定义核心 Node 类型、属性、状态机和关系模型（至少覆盖 Satellite、GroundStation、Mission、Task、Community）
- 设计 Knowledge Graph 的基础存储模型与查询接口
- 定义核心事件 schema（至少 10 个核心事件），并输出 schema 文档
- 实现 Node 的注册、更新、删除、查询功能
- 实现基础关系维护能力（如 Satellite belongsTo Community、controlledBy GroundStation）
- 在 Node 模型中预留分片相关字段和联邦标识字段

**验收标准**：
- 能够通过 API 或事件完成 Node 的完整生命周期管理
- 事件 schema 文档清晰、版本化，并支持后续扩展
- 领域模型设计文档中包含“扩展性考虑”章节

**依赖**：无

### 5.2 事件驱动框架（Event Bus）

**任务目标**：构建本地事件发布与订阅能力，并为未来升级为分层 Event Mesh 做好准备。

**具体子任务**：
- 实现基础 Event Bus（支持发布、订阅、持久化）
- 设计 Event Bus 的抽象接口层（便于后期替换为分布式消息中间件）
- 支持事件 schema 校验与版本管理
- 实现事件持久化与重启恢复能力
- 实现基础的事件回放功能（用于调试和审计）

**验收标准**：
- 能够可靠地发布和订阅事件
- Event Bus 抽象层设计文档完成
- 支持事件持久化与重启后恢复

**依赖**：领域模型基础（事件 schema）

### 5.3 对象模型持久化与查询

**任务目标**：为 Node 和 Knowledge Graph 提供持久化能力，并设计支持未来分片的存储抽象。

**具体子任务**：
- 选择并集成合适的存储方案（关系数据库 + 图数据库混合）
- 实现 Node 的 CRUD 操作和基础关系查询
- 设计存储抽象接口层（支持后续迁移到分布式存储）
- 实现基础的 Knowledge Graph 查询能力

**验收标准**：
- Node 数据能够持久化并可靠查询
- 存储抽象层设计完成，具备可替换性
- 查询性能满足单机基础使用场景

**依赖**：领域模型定义

### 5.4 Telemetry Pipeline

**任务目标**：实现遥测数据的接收、解析与事件发布，并支持逻辑分片设计。

**具体子任务**：
- 实现遥测接收接口（支持常见协议）
- 设计基础遥测解析框架（支持配置化解析）
- 将解析后的遥测发布为 `TelemetryReceived` 事件
- 实现基础缓存与流控机制
- 在 Pipeline 中预留按卫星/区域/Community 进行逻辑分片的扩展点

**验收标准**：
- 能够接收并解析遥测数据
- 遥测数据能够转化为标准事件
- Pipeline 设计文档中包含逻辑分片扩展点说明

**依赖**：事件驱动框架、领域模型

### 5.5 Command Pipeline

**任务目标**：实现遥控指令的队列、发送与确认机制。

**具体子任务**：
- 实现命令队列与优先级管理
- 实现命令发送、重试与超时处理机制
- 实现命令确认与状态反馈
- 基础安全检查（权限、格式校验）

**验收标准**：
- 能够下发命令并收到确认或失败反馈
- 支持命令重试与超时处理

**依赖**：事件驱动框架、领域模型

### 5.6 Plugin Framework 基础

**任务目标**：构建插件加载与运行的基础框架。

**具体子任务**：
- 实现插件生命周期管理（加载、启动、停止、卸载）
- 定义基础扩展点（至少包含协议适配、事件订阅扩展点）
- 实现插件元数据解析与简单依赖管理
- 插件能够通过事件与核心系统交互

**验收标准**：
- 能够动态加载和运行协议插件
- 插件能够订阅和发布事件
- 扩展点设计文档完成

**依赖**：事件驱动框架

### 5.7 基础 SDK 与 CLI

**任务目标**：提供开发者友好的接入方式。

**具体子任务**：
- Python SDK 开发（核心对象操作、事件发布订阅、Node 查询）
- Go SDK 开发（可选，优先级次于 Python）
- CLI 工具开发（节点注册、事件查看、简单任务下发、状态查询）

**验收标准**：
- 开发者能够通过 SDK 快速接入核心能力
- CLI 工具能够完成基础运维操作

**依赖**：领域模型、事件驱动框架

### 5.8 Identity 与权限基础

**任务目标**：提供基础的身份认证与权限控制。

**具体子任务**：
- 基础用户与角色模型
- 简单的 RBAC 实现
- Node 级别的权限控制基础（例如只能操作自己拥有的卫星）

**验收标准**：
- 能够对不同用户进行权限区分
- 关键操作能够进行权限校验

**依赖**：领域模型

### 5.9 部署与基础运维

**任务目标**：支持单机部署和基本运维。

**具体子任务**：
- Docker 镜像制作
- docker-compose 单机部署方案
- 基础日志、指标与监控集成（OpenTelemetry 基础支持）
- 配置管理基础能力

**验收标准**：
- 能够在 Windows 和 Linux 上通过 Docker 快速部署
- 提供清晰的部署文档

**依赖**：各核心模块基本完成

---

## 6. 接口与契约要求

- 所有核心能力必须通过 API（REST/gRPC）和事件进行暴露。
- 核心事件 schema 必须版本化管理，并提供清晰文档。
- 对象模型变更必须通过 RFC 流程。
- Plugin Framework 的扩展点必须提前定义并保持稳定。
- 所有与扩展性相关的抽象层（Event Bus、存储、Pipeline）必须输出设计文档。

---

## 7. 质量与测试要求

- 单元测试覆盖率不低于 70%（核心模块力争 80% 以上）。
- 集成测试覆盖主要业务流程。
- 必须提供完善的开发者文档和示例。
- 每个阶段结束需进行架构评审（重点评审扩展性设计）。

---

## 8. 里程碑与交付清单（Phase 1）

| 里程碑 | 时间建议 | 主要交付物 | 验收标准 |
|--------|----------|------------|----------|
| M1 | 第 5 周 | 领域模型 + 事件 schema + 扩展性设计文档完成 | 文档清晰，包含扩展性考虑 |
| M2 | 第 9 周 | 事件驱动框架 + 对象模型持久化完成 | 能够发布订阅事件并持久化 Node |
| M3 | 第 13 周 | Telemetry + Command Pipeline 完成 | 能够完成遥测接收和简单遥控 |
| M4 | 第 17 周 | Plugin Framework + SDK + CLI 完成 | 支持插件加载，可通过 SDK 开发 |
| M5 | 第 19 周 | MVP 集成与部署完成 | 可独立部署并完成基础闭环 |

---

## 9. 风险与依赖

**主要风险**：
- 领域模型设计不合理导致后期大规模重构（缓解：早期充分评审 + 明确扩展性要求）。
- 事件驱动架构在高并发下的性能问题（缓解：早期进行性能验证 + 抽象层设计）。
- 插件框架设计过于复杂导致采用率低（缓解：保持简单，逐步增强）。

**关键依赖**：
- 领域模型的早期定义质量直接影响后续所有模块。
- 事件 schema 的稳定性对插件生态至关重要。
- 扩展性设计的质量直接影响 Phase 2/3 的实施难度。

---

**文档结束**

*本任务书（v1.1）为细化版本，在 v1.0 基础上加强了可扩展性设计要求和任务粒度，作为 Openspace OS Core 开发的唯一基准。*